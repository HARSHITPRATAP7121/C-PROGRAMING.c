#include<stdio.h>
int main()
{
  int a, b;
  printf(" enter a value :");
  scanf("%d""%d",&a,&b);
  if(a<b)
  {printf("small");}
  else
  if(a==b)
  {printf("equal");}
  else
  {printf("big");}
  return 0;
}